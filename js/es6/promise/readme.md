# 异步
- 进程：cpu接受一个指令，到加载完上下文环境所需要的时间
- 线程：cpu执行指令所需的时间


- js默认是单线程运行的，一份代码，（ v8 默认只会开一个主线程来跑 js 代码）
1. 因为js设计之初是浏览器的脚本语言，设计成单线程可以节约用户设备性能
2. js代码中存在耗时执行的，会被v8引擎挂起，先执行不耗时的，也就称之为同步。


# 处理异步
1. 回调函数（call back）：在耗时函数中调用不耗时函数。
   - 回调地狱，当函数嵌套过深，使得代码难以维护。
2. promise: