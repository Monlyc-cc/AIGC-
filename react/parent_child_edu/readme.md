# 客户端
- 移动端
1. 适配不同屏幕尺寸
 - 不同屏幕尺寸页面元素的排版
 - 不同屏幕尺寸元素的大小 （rem）
 - 封装rem.js 用来动态修改页面根字体大小

 2. 样式初始化
 
 3. less.js
 - 使用less的方法写css
 
 4. nom i react-router-dom
 ## npm i 作用 
 默认会将pack.json中记录的依赖全部安装


 # npm init -y
 初始化一个项目，生成一个package.json文件


 # 服务端

 - 定义多个接口：
 1. npm i koa-router 安装路由，用来分门别类的定义后端接口地址
 2. 后端开发准则 
 - 代码分层
    1. 路由层,专门用来声明路由借口
    2. 控制层
    3. 模板层

3. npm i koa-bodyparser 辅助koa解析 post 请求体中的参数